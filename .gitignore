# yohan-ai-dictionary — public site, own repo (Phase 3 template); not part of private Yohan OS snapshot
# leading slash = repo-root only, so SoT 경로 memory/projects/yohan-ai-dictionary/는 추적된다
/yohan-ai-dictionary/

# yohan-profile-card — separate Next.js side project, own repo; not part of private Yohan OS snapshot
/yohan-profile-card/

# Claude Code / agents — local-only (단, soul 상속 산출물·skills 는 추적)
.claude/
.agents/*
!.agents/skills
!.agents/SOUL.md
!.agents/CORE-RULES.md

# Cursor MCP — 머신별 경로·시크릿 (example 만 커밋)
.cursor/mcp.json
!.cursor/mcp.json.example

# SoT 스냅샷/로컬 헬퍼 — 기기마다 다름
sot-snapshot.md
generate-snapshot.ps1
skills-lock.json
ntn-install.sh

node_modules/
dist/
.env
.env.*
!.env.example
*.local

# 텔레그램 봇 단일 인스턴스 락
memory/.telegram-bot.lock

# Tesseract 학습 데이터(로컬에 두었을 때만; tesseract.js는 기본적으로 캐시에 다운로드)
*.traineddata
git-autopull.log
*.log
.DS_Store
Thumbs.db
desktop.ini
~$*

# 에이전트/배치 런타임 상태 (대시보드 SSE 소스)
memory/metrics/automation/agent-status.json

# 로컬 커밋 메시지 초안
.commit-msg-tmp.txt
