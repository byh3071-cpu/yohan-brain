# 요한 운영체계 v0.1 — 설계 문서 (2026-07-02, 초안)

> 1인 + 멀티 AI 기업의 운영체계. "AI가 일하기 좋은 구조, 나 없이도 돌아가는 시스템 > 제품·수익화" (백요한 선언, 2026-07-02 — 6/30 '루프 먼저' 결정을 공식 개정).
> **결정 방식**: 항목마다 `현황(사실) → 선택지 → 추천 → ☐ 결정` — 결정란은 백요한만 채운다. 이 기록이 곧 검수.

## 0. 확정된 결정 (2026-07-02 세션)

| ID | 결정 | 상태 |
|---|---|---|
| R0 | 6/30 닻 문서("다 짓지 말고 한 바퀴부터") **공식 개정** — 평일=운영체계 구축, 주말=콘텐츠 루프 유지. 노션 원문에 개정 블록 추가 | ✅ 확정 (노션 반영 대기) |
| R1 | 트랙 순서 **A(신뢰) → B(규칙) → C(지식) → D(지휘)** | ✅ 확정 |
| R2-1 | 트랙 A 첫 조각 = 밤샘 멈춤 수사 + `vhk watch` (수준 2: 감지+텔레그램) | ✅ 구현 — vhk PR #441 |
| R2-보조 | watchdog 수준 2로 출시 → 수사에서 "재시도 무해 원인"으로 판명된 케이스만 수준 3(자동 재개) 승격. 원인 불명 멈춤은 영원히 수준 2 | ✅ 확정 |
| 채널 분리 | 사람 알림=텔레그램, 에이전트 협업 공간=디스코드(atelier-ai), 슬랙 도입 안 함. 감시자는 에이전트가 아닌 경량 폴러 | ✅ 확정 |

## 1. 원칙 (사건에서 추출한 헌법 보강)

1. **silent fallback 금지** — 입력이 안 오면 기본값으로 조용히 진행하지 말고 죽어라 (7/1 밤샘루프: Workflow args 폴백 → 오실행 2회 → PR 6개 부산물).
2. **무인 가동에는 진행 신호 의무** — 사람이 아침까지 모르는 상태를 만들지 않는다 (`vhk watch`).
3. **완료·멈춤 구분 불가 시 정직하게 둘 다 알림** — 아는 척 금지 (receipt 철학과 동일).
4. **측정 먼저, 확장 나중** — RAG·자동화·시스템 전부 (vhk measure-first 독트린 계승, 범용 임베딩 회의론 쓰레드 2026-07-02 박제 예정).
5. **낡은 기록은 회수 시 검증** — 문서 기반 보고 전 코드·깃 상태와 대조 (2026-07-02 "VHK G1을 P0로 오보고" 사건).

## 2. 4트랙 구조

| 트랙 | 정의 | 핵심 자산 | 최대 갭 |
|---|---|---|---|
| A. 신뢰 | AI 산출물을 믿을 수 있게 | vhk receipt T1·verify 5게이트·watch(#441) | 검수 표준, receipt T2·T3, 멈춤 원인 확정 |
| B. 규칙 | 누가 와도 같은 규칙으로 | vhk sync(RULES.md→8개 도구 파일)·soul.yaml·core-ruleset+inject_core_rules | **전역↔레포 두 파이프라인이 서로 모름** (합류 지점 0) |
| C. 지식 | 기억이 썩지 않게 | 노션 54-DB·brain memory/(688파일)·notion↔brain 동기 4도구·get_context | SSoT 이행 로드맵, 신선도 장치 0 |
| D. 지휘 | 여러 AI를 한 사람이 지휘 | atelier-ai Phase A·ccc/hcc 런처·tmux+worktree 가이드 | 분배·수거 규약 없음 (A·B 후 착수) |

## 3. R2 잔여 — 트랙 A 세부 결정

### A-1. 사람 검수 표준 (승인 전에 보는 것)
- 현황: 검수가 최대 병목(백요한 진술). 지금은 비정형 — 뭘 보고 승인할지 문서화된 기준 없음.
- 선택지 —
  - **(a) 증거 패키지 3종 고정**: ① receipt(기계 증거: 게이트 PASS·mission 대조) ② diff 요약(변경 파일·핵심 hunk 한국어 요약) ③ 실행 증거(테스트 출력·실기동 스크린샷/로그). AI는 이 3종을 항상 같은 형식으로 제출, 사람은 이 3종만 봄.
  - (b) receipt 단독 신뢰 (기계 판정 위임 확대)
- 추천: **(a)** — receipt는 "게으른 거짓완료"를 못 잡는다고 스스로 문서화돼 있음(RFC 0056 정직화). 기계+눈 이중화가 현 단계에 맞음.
- ☐ 결정:

### A-2. receipt T2(위조방어)·T3(적발 입증) 착수 시점
- 현황: T1 라이브. 90일 성공기준 = 거짓완료 적발 1건(현 0/8).
- 선택지: (a) 이번 주 내 T2 착수 / (b) watch 가동 1주 관찰 후
- 추천: **(b)** — A-1 표준이 먼저 서야 T2가 "뭘 위조로 볼지" 정의가 정확해짐.
- ☐ 결정:

### A-3. watch 수준 3 승격 조건
- 현황: 수준 2 출시(#441). 멈춤 순간 원인은 집 PC 로그 확인 대기.
- 기준(제안): 같은 원인 멈춤 3회 관찰 + 재시도 부작용 없음 문서화 → 그 원인만 자동 재개 화이트리스트.
- ☐ 결정:

## 4. R3 — 트랙 B 세부 결정 (규칙 SSoT)

### B-1. 전역 규칙의 SSoT 위치
- 현황: 전역 후보 2곳이 이미 존재 — ① yohan-brain `memory/core/core-ruleset.yaml`(+soul.yaml, `inject_core_rules`로 각 레포 `.agents/CORE-RULES.md` 주입) ② yohan-cc-skills(플러그인 배포망, 멀티머신 동기). 레포 규칙은 각 레포 `RULES.md`(vhk sync 원본).
- 선택지 —
  - **(a) yohan-brain을 전역 SSoT로 확정** — core-ruleset.yaml이 원본, cc-skills는 배포 채널로 강등(브레인이 SSoT라는 C트랙 비전과 정합).
  - (b) yohan-cc-skills를 원본으로 (배포망 = 원본 동일체)
- 추천: **(a)** — "SSoT는 한 곳" 원칙 + 브레인 비전 정합. cc-skills는 컴파일 산출물만 나름.
- ☐ 결정:

### B-2. 계층 합성 방식 (전역 규칙이 8개 도구 파일에 어떻게 들어가나)
- 현황: vhk sync는 레포 RULES.md만 파싱 → CLAUDE.md·AGENTS.md·.cursorrules 등 8파일 생성(마커로 자동영역 관리, 드리프트 감지 완비). 전역(core-ruleset)은 별도 파일(.agents/CORE-RULES.md)로 주입돼 **8파일에 합류 안 됨**.
- 선택지 —
  - **(a) RULES.md에 전역 섹션 주입**: `inject_core_rules`(또는 vhk 신규 명령)가 각 레포 RULES.md 상단에 `## 전역 규칙 (자동 주입 — 편집 금지)` 마커 섹션을 삽입 → 기존 vhk sync가 그대로 8파일로 컴파일. **vhk sync 무수정**, 합류 지점은 RULES.md 하나.
  - (b) vhk sync에 --global 소스 옵션 추가 (sync가 전역 파일을 직접 읽어 병합) — vhk 수정 필요, 합성 로직이 sync 안으로.
- 추천: **(a)** — 기존 부품 무수정 연결(벌려놓은 것 닫기 원칙), 마커 패턴도 이미 검증됨(CLAUDE.md 하이브리드와 동일 기법).
- ☐ 결정:

### B-3. 충돌 시 우선순위
- 제안: **레포 규칙 > 전역 규칙** (구체가 일반을 이김 — 단 soul.yaml guardrails 10개는 절대 우선, 레포가 못 뒤집음).
- ☐ 결정:

### B-4. 전파 트리거
- 선택지: (a) 수동 `vhk sync` (현행 유지) / (b) 세션 시작 hook에서 드리프트 감지 시 안내(vhk BACKLOG L2 #2와 합류) / (c) boot-auto-pull side task로 주기 실행
- 추천: **(b)** — 자동 쓰기는 위험(사람 승인 유지), 감지·안내까지만 자동.
- ☐ 결정:

### B-5. 도구 교체 내성 (탈종속 플랜)
- 현황: vhk sync SYNC_TARGETS가 이미 8포맷(Cursor·Codex/AGENTS·Copilot·Gemini·Cline·Windsurf·Antigravity·Claude). 새 도구 = 생성기 1개 추가.
- 제안: "메인 도구 교체 리허설"을 분기 1회 — 코덱스(AGENTS.md)만으로 작은 작업 1건을 끝까지 수행해 규칙·맥락이 실제로 전달되는지 확인. 문서가 아니라 실행으로 검증.
- ☐ 결정:

## 5. R4 — 트랙 C 세부 결정 (지식 SSoT)

### C-1. ADR-006 개정 (SoT 3축 → 브레인 중심)
- 현황: ADR-006 = "노션이 지식·기획 정본, Git이 코드 정본, memory/는 에이전트 런타임". 백요한 새 비전 = **브레인(git)이 최종 SSoT, 노션은 사람용 뷰+백업**.
- 선택지 — (a) 즉시 개정(ADR-009 신규로 ADR-006 supersede) / (b) 이행 실적(C-2) 1단계 완료 후 개정
- 추천: **(b)** — ADR은 실태를 따라가야지 소망을 앞서가면 또 "낡은 문서"가 됨. 1단계 이행이 실제로 돌고 나서 박제.
- ☐ 결정:

### C-2. 이행 1단계 범위 (뭐부터 브레인으로)
- 선택지 —
  - **(a) decisions부터**: 이미 brain `memory/decisions/`(35건)+`notion_push_decisions` 양방향 도구가 있음 — "결정은 브레인에 먼저 쓰고 노션으로 push"로 방향만 확정하면 즉시 가동.
  - (b) SUMMARY(수백 건)부터 — 볼륨 커서 이행 비용 큼
  - (c) 가이드/wiki부터
- 추천: **(a)** — 부품 완비 + 오늘부터 실천 가능 + "지난 결정" 회수가 제일 잦은 유즈케이스.
- ☐ 결정:

### C-3. 신선도(낡음) 규칙
- 제안(초안) —
  - 판정: 문서 frontmatter에 `verified: <날짜> <기준커밋>` 스탬프. 회수 시 (지금 - verified) > 14일이면 "낡음 의심" 라벨을 달고 인용.
  - 처리: 낡음 의심 문서를 근거로 계획·보고할 땐 **코드·깃 상태와 대조 후 인용** (원칙 5). 대조 결과가 다르면 문서에 `superseded-by:` 스탬프.
  - 자동화는 여기까지 — 삭제·아카이브는 사람만.
- ☐ 결정:

### C-4. 벡터 인프라(control-tower) 재개 조건 (기존 결정 재확인)
- 확정 유지: 루프를 돌리며 실질의가 쌓인 뒤(그 질의가 eval 셋) + dense 단독 금지(하이브리드) + Recall 측정 먼저. auto-fix PR 4개 리뷰는 트랙 A 백로그로 이관.
- ☐ 재확인:

## 6. R5 — 트랙 D 개요 (A·B 이후)

- 지휘 규약 스케치: 분배(작업 계약 = vhk mission 포맷 재사용) → 실행(tmux pane / worktree 격리) → 수거(receipt+증거 패키지 A-1 형식) → 판정(사람).
- 헤르메스·오픈코드 평가는 "판단형 재개·자율 분배"가 필요해지는 시점에 — 지금 도입 안 함.
- atelier-ai Phase B(사옥)는 트랙 D의 실험장 — A·B 안정 후.
- ☐ 방향 동의:

## 7. 트랙별 다음 3조각 (백로그)

| 트랙 | 1 | 2 | 3 |
|---|---|---|---|
| A | 집PC 멈춤 원인 확정+autoloop fail-fast (**Stop hook 8연속 차단 종료 여부 확인** — 축2 발견) | 검수 표준(A-1) 문서화+vhk 통합 | receipt T2 |
| B | B-2 합류 파일럿 1레포(vhk 또는 brain) | 전 레포 확산+드리프트 감지 | 교체 리허설(B-5) |
| C | decisions 이행 1단계(C-2) | 신선도 스탬프(C-3) 파일럿 | ADR-006 개정(C-1) |
| D | tmux+worktree 실전 1회(오늘 저녁 길2) | 지휘 규약 초안 | atelier Phase B 재개 판단 |

## 8. 외부 근거 (딥리서치 2026-07-02 — 23개 주장 전원 적대검증 3-0 생존, 105 에이전트)

### 축1 규칙 SSoT — 업계 2패턴, 그리고 AGENTS.md 수렴
- **Ruler**(intellectronica/ruler, 2.8k★): `.ruler/` 원본 → 32종 에이전트 설정 생성·배포(파일별 Source 추적 마커). **vhk sync 와 동일 아키텍처** — 우리 B-2(a)안의 업계 대응물.
- **SSW 심링크형**: AGENTS.md 단일 원본 + `ln -s AGENTS.md CLAUDE.md` 등 심링크.
- 지형: Cursor·Copilot·OpenCode 는 AGENTS.md 네이티브, **Claude Code 만 CLAUDE.md 홀드아웃**(공식 문서가 심링크/@AGENTS.md import 워크어라운드 권장), `.cursorrules` 사실상 사망(deprecated). AGENTS.md 는 Linux Foundation 기부(2025-12)·6만+ 프로젝트.
- **부패 실증**: steipete/agent-rules(5.7k★)가 생성 11개월 만에 저자 스스로 아카이브("AI moves fast. This was old stuff") — 정적 규칙 컬렉션은 ~1년 내 썩는다. B-5(교체 리허설)의 직접 근거.

### 축2 검증 게이트 — Anthropic 공식 처방
- 에이전트는 "looks done"에서 멈춘다 → 사람이 verification loop 가 되지 말고 **실행 가능한 pass/fail 체크로 루프를 닫아라** (테스트·빌드 exit code·스크린샷 비교).
- 무인 실행 공식 사다리: 프롬프트 내 체크 → **/goal**(작업 모델과 분리된 fresh evaluator 가 매 턴 조건 재검사) → **Stop hook**(통과까지 턴 종료 차단, **8회 연속 차단 시 override 후 종료** — 무한루프 하드리밋).
- 승인 전 검수: fresh-context 적대 리뷰 + **성공 주장 대신 증거 요구**(테스트 출력·실행 명령·스크린샷) — A-1(a) 증거 패키지와 정확히 일치.

### 축3 개인 지식베이스 — 공개 레퍼런스 2종
- **Karpathy LLM Wiki**(2026-04 gist): 3계층 — raw sources(LLM 읽기전용) / LLM 이 전적으로 소유·작성하는 wiki / **schema 를 CLAUDE.md·AGENTS.md 에 저장**. 부패 방지 = **lint 오퍼레이션**(모순·superseded·orphan 주기 감사). ※ brain ADR-007 이 이미 이 패턴 채택 — C-3 신선도 규칙은 lint 의 우리식 이름.
- **Simon Willison til**: 주제 폴더 115개·579 엔트리, **CI 가 인덱스·검색 사이트를 파생 생성 — repo 가 SSoT, 뷰는 빌드 산출물**. C-1 "브레인=SSoT·노션=뷰" 비전의 실존 증명.

### 축4 병렬 오케스트레이션
- Claude Code **네이티브 worktree**(2026-02-20 제품화): `--worktree`, `.claude/worktrees/`, `.worktreeinclude`.
- **workmux**(1.7k★): tmux+worktree+에이전트 상태 대시보드 통합 — 오늘 저녁 길2(WSL2+tmux) 셋업 시 평가 후보.

### 우리 결정과의 정합 요약
| 우리 결정 | 업계 대응 | 판정 |
|---|---|---|
| B-2(a) RULES.md 컴파일 | Ruler 동형 | ✅ 주류 패턴 |
| A-1(a) 증거 패키지 | Anthropic "증거 요구" | ✅ 공식 일치 |
| C-3 신선도 | Karpathy lint | ✅ 개념 검증됨 |
| watch(감지·알림) | Stop hook 사다리와 상보 (hook=차단, watch=관찰) | ✅ 중복 아님 |
| ⚠️ 신규 시사점 | 원본 포맷을 AGENTS.md 로 수렴시키는 흐름 — RULES.md 원본 유지 여부는 B-2 파일럿 후 재평가 | 관찰 |

## 9. 이 문서의 신선도

- verified: 2026-07-02 (vhk 83fe7a1 · brain 8a2833d 기준 실조사)
- 다음 검증 기한: 2026-07-16 (C-3 규칙 자체 적용 1호)
