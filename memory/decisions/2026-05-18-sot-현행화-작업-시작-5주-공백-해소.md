---
title: "SoT 현행화 작업 시작 — 5주 공백 해소"
created: 2026-05-18
source: "codex"
tags: [sot, revival, memory, harness]
---

## 배경

2026-04-10 이후 약 5주간 Yohan OS의 SoT 갱신 공백이 있었다. 이로 인해 `get_context`가 2026-05 현재 맥락을 정확히 반환하도록 `memory/`와 루트 진입 문서를 다시 정렬할 필요가 생겼다.

## 결정

- `memory/active-project.yaml`을 `yohan-os-sot-revival` 작업으로 갱신한다.
- `memory/inbox/`에서 30일 이상 된 파일을 `memory/inbox/archive/`로 이동해 활성 인박스를 정리한다.
- 루트 `CLAUDE.md`를 생성해 Claude 진입점도 `AGENTS.md`, `memory/profile.yaml`, `memory/active-project.yaml`을 보도록 정렬한다.
- 이번 작업을 decision 로그로 남겨 5주 공백 이후의 재개 지점을 명확히 한다.

## 후속 확인

- `get_context`가 갱신된 active project와 최근 decision을 반환하는지 확인한다.
- `memory/profile.yaml` 일부 텍스트의 인코딩 깨짐은 별도 정리 후보로 남긴다.
