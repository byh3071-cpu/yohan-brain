---
title: 대시보드 문서 경로 하드닝 (VHK Goal 1)
created: 2026-06-07T08:31:25.022Z
source: mcp.append_decision
---

VHK Goal 1로 대시보드 문서 경로 하드닝 완료 — getMemoryDir, DOC_SOURCES, preview race fix, verify 게이트

## 배경
코드리뷰 후속: MEMORY_ROOT cwd 의존, doc-preview race, 경로 상수 중복, 테스트 부재.

## 결정
- VHK Goal 1 (`goals/1-dashboard-doc-path-hardening.md`) 4 Unit으로 처리
- `dashboard/src/lib/paths.ts` — `YOHAN_OS_ROOT` + memory 존재 probe
- `DOC_SOURCES` 단일 SoT → prefix·category derive
- `doc-preview` AbortController + status별 에러 메시지
- `scripts/verify-dashboard-doc-paths.ts` + `check-goal-1.mjs` 게이트

## 검증
- `vhk goal check --id 1` 통과 (2026-06-07)
- `vhk goal done --id 1` → DONE
