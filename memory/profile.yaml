# =============================================================================
# [DEPRECATED] profile.yaml — soul.yaml 로 흡수됨 (2026-06-18)
# =============================================================================
# 사용자 정체성·일하는 방식·선호·금칙(가드레일)의 SSoT는 이제 memory/soul.yaml 이다.
# 노션 SOUL 설계실에서 렌더 → soul.yaml. 직접 편집 금지(노션에서 다듬고 재렌더).
#
# - 이 파일의 모든 고유 필드는 soul.yaml 로 병합됨.
# - get_context 는 이제 soul.yaml 을 읽어 payload.soul 로 서빙한다 (profile 은 이 스텁).
# - SoT 단일성: 새 프로필 필드를 여기에 추가하지 말 것 → soul.yaml(=노션 SOUL)에만.
#
# 마커 주입(.agents/SOUL.md)·런타임 배선은 scripts/inject-soul.mjs / get_context 참고.
# =============================================================================
deprecated: true
superseded_by: memory/soul.yaml
note: "프로필 SoT는 soul.yaml로 이전됨. 이 파일은 하위호환 리다이렉트 스텁."
